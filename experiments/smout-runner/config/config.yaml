defaults:
  - runner_schema
  - pre-opt: Oz
  - smout: full
  - alive2: none
  - post-opt: default
  - compile: default
  - machine_outliner: "yes"
  - _self_
  #- override hydra/launcher: joblib

files:
  input_bc: ${oc.env:PWD}/ppmtomitsu.bc

calls:
  input:
    func: bcdb.split
    args:
      - ${var:input_bc}
